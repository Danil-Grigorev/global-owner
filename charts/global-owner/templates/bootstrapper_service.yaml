apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default .Release.Name }}-bootstrap
  namespace: {{ .Values.namespace | default .Release.Namespace }}
spec:
  selector:
    app: {{ .Values.name | default .Release.Name }}-bootstrap
  ports:
  - port: 80
    targetPort: 8080