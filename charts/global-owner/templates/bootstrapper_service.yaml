apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-bootstrap
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-bootstrap
  ports:
  - port: 80
    targetPort: 8080