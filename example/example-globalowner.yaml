apiVersion: globalowner.metacontroller.io/v1alpha1
kind: GlobalOwner
metadata:
  name: global-owner
spec:
  selector:
    matchExpressions:
    - {key: adopt, operator: Exists}
  childResources:
  - apiVersion: v1
    weight: 1
    resource: secrets
  - apiVersion: v1
    resource: configmaps
